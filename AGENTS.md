# ゲーム実況の半自動編集

- 目的：JSON編集プランを人が確認・修正し、別の動画にも再利用できる編集環境を作る。
- **input/ と元素材は読み取り専用。変更・上書き・削除・ハードリンク経由の書き込みは禁止。**
- 新しい生成物は work/、完成・プレビュー動画は output/。以前の outputs/ と既存Pythonコードは保持する。
- Pythonは解析・抽出・文字起こし・候補生成、TypeScript/React/Remotionはタイムラインと映像演出を担当する。
- work/edit_plan.json が中心。解析とレンダラーを分離し、enabled=false の候補は自動適用しない。
- 既存エフェクトを再利用する。追加時は型、強度0〜1、フレーム依存の決定的動作、docs/effects.md、短い検証例をそろえる。
- 全編レンダリング前に短いプレビューを確認する。元動画の指紋を処理前後に比較し、尺・コーデック・主要フレームを検証する。
- 詳細仕様の正本は docs/。コマンドを変えたらREADMEも更新する。
