# ゲーム実況の半自動編集

## 目的と保護
- 「input/動画.mp4 を編集して」は、指定動画の字幕・テンポ調整・リアクション演出・完成MP4作成の依頼として扱う。個別の範囲指定（例：30秒だけ、全編出力しない）を最優先する。
- **input/ と元素材は読み取り専用。変更・削除・上書き・ハードリンク経由の書き込みは禁止。** 作業物はwork/、完成・プレビューはoutput/。旧outputs/と既存コードを保持する。
- Pythonは解析、Remotionは映像表現、FFmpegは抽出・前処理・音声処理。既存実装を再利用し、プロジェクト再構築や新しい解析実装を毎回行わない。

## 標準手順（指定動画を必ず明示）
1. package.json・対象ファイル・既存プランの対象を確認。`python scripts/prepare_edit.py "input/動画.mp4" --check` で実行予定を確認する。
2. `python scripts/prepare_edit.py "input/動画.mp4"` を実行する。内部で **scripts/transcribe.py** →既存解析→初期プラン生成を行う。別動画の既存プランは `--replan` でバックアップ後に再生成。同じ動画の手修正は保護し、必要なときだけ再生成する。
3. 文字起こしは必ず専用ツールを優先。単独実行は `npm run transcribe -- "input/動画.mp4"`。既存ローカルモデル・同一入力キャッシュを再利用し、別のWhisper処理を重ねない。全編依頼に `--duration 30` の部分結果を流用しない。モデル・入力・完全性はwork/transcribe_cache.jsonで確認する。手修正済み字幕は退避し、自動再生成で失わない。
4. work/edit_brief.jsonから尺・候補・素材の要約を読み、必要な時間帯だけ字幕と映像・音声を確認する。音量やキーワードだけで失敗・移動・重要度を確定しない。テスト用の「マジか！！」等を本番の発言として残さない。
5. 通常字幕は **work/transcript.json**、編集演出は **work/edit_plan.json**。通常字幕を全件プランへコピーしない。時刻は元動画の秒数。既存コンパイラにカット・速度変更後のフレーム変換を任せる。
6. 下記方針で候補を選び、判断理由をプランに残す。根拠を確認できたものをenabled=trueにし、未確定はfalseで保持。毎回ユーザー承認を要求せず、依頼範囲で編集判断を進める。判断できない重要箇所は原映像を残す。
7. `npm run validate` → `npm run preview -- --seconds 30`。必要な演出区間は `--start 秒 --seconds 15` で追加確認。字幕・音声同期・重なり・ゲームUI・カット境界を確認して修正する。
8. 全編出力が依頼範囲ならプレビュー確認後に `npm run render` を一度実行。output/final.mp4を1080p/H.264/AAC、元FPSを尊重して出力。原本ハッシュ、尺、音声、主要フレームを確認して納品する。

## 編集方針と既存機能
- 通常字幕：発話の大部分に表示。1〜2行、下部中央、白太字＋黒縁、控えめな動き。口語・反応語を残し、誤認識を確認。強調字幕と領域を分け、ゲームUIを隠しすぎない。
- 不要な間：1.2秒以上をcut候補。前後の発言・笑い・驚き・重要操作を切らず、内容をなるべく残す。
- リアクション・重要発言：大きなImpactCaption（type:caption / effectのcaption）を選択的に追加。punchZoomは短く、screenShakeは強い反応だけ、flashはごく短く薄くする。
- 失敗・笑い：freeze、monochrome、fail / funnyFailを選択的に利用。フリーズ中の音声は無音になるため発言を欠かさない位置に置く。
- 見せたい瞬間：speedのrate=0.5〜0.8でスロー。単調な移動・作業：rate=1.5〜3で倍速。会話や重要操作は通常速度を優先。
- SE：assets/seの適切な短い音声をtype:seで配置。BGM：assets/bgmを低音量のtype:bgmで配置し、実況・リアクションを優先して減衰。素材がなければ省略して報告。勝手に素材をネット取得しない。
- 画像・動画：必要な場合のみassets/images・assets/videosからtype:overlayで重畳。視認性を優先。
- 冒頭ダイジェスト：確認した見どころ1〜3箇所、合計5〜8秒をdigest.clipsへ指定しenabled=true。本編の同じ場面も残す。文の途中で切らず、速度変更後の実尺も確認。適切な候補がなければ無理に挿入しない。
- 14種類の要素を毎回すべて使う必要はない。派手だが見づらくない強度にし、同じ演出の連発や意味のない効果を避ける。config/editing.json・config/effects.jsonを再利用。

## トークンと再実行の節約
- 文字起こし・時刻変換・無言検出・音量計算は固定ツールに任せる。LLMで再計算したり、既存ツールを毎回書き直さない。
- 最初は件数・尺・上位候補だけ読む。全文JSON・全ログ・全フレームを一括で取り込まず、判断に必要な区間だけ抽出する。
- 同一入力の音声・文字起こし・作業用動画・検証結果を再利用。変更のない解析や全編レンダリングを繰り返さない。
- 新しいエフェクトは既存部品で足りない場合のみ追加し、型・0〜1の強度・決定的なフレーム計算・短い検証例をそろえる。詳細はdocs/、コマンド変更はREADMEへ反映する。
